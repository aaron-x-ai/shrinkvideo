version: 1
# Documentation-only examples (not loaded at runtime in P0).
examples:
  - title: Downloads inbox
    input_dir: ~/Downloads/VideoInbox
    output_dir: ~/Downloads/VideoShrunk
    glob: "*.{mp4,mov}"
