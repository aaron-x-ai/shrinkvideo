version: 1
# Documentation-only examples (not loaded at runtime).
inbox_examples:
  - title: 视频压缩中转
    staging_dir: ~/Movies/ShrinkVideo-Inbox
    output_dir: ~/Movies/ShrinkVideo-Done
    glob: "*.{mp4,mov,mkv}"
